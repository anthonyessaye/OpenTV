name: opentv_core
description: Provider-agnostic domain core for OpenTV — playlist, EPG and Xtream parsing.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
