name: opentv_core
description: Provider-agnostic domain core for OpenTV — playlist, EPG and Xtream parsing.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  cryptography: ^2.9.0
  drift: ^2.20.0
  xml: ^6.5.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.20.0
  lints: ^5.0.0
  sqlite3: ^2.4.0
  test: ^1.25.0
